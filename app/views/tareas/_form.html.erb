<%= form_for(@tarea) do |f| %>
  <% if @tarea.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tarea.errors.count, "error") %> prohibited this tarea from being saved:</h2>

      <ul>
      <% @tarea.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tarnombre %><br>
    <%= f.text_field :tarnombre %>
  </div>
  <div class="field">
    <%= f.label :tarestado %><br>
    <%= f.text_field :tarestado %>
  </div>
  <div class="field">
    <%= f.label :tarfechacreacion %><br>
    <%= f.date_select :tarfechacreacion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
